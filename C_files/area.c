#include <stdio.h>

int main() {
    int length, breath;
    printf("Enter the length of the area :");
    scanf("%d",&length);
     printf("Enter the breath of the area :");
     scanf("%d",&breath);
     printf("\nArea of the surface is %d.\n",length*breath);
    return 0;
    }