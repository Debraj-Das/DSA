//Name Debraj Das
//Roll no-21CH30010

#include <stdio.h>

int main()
{
    float a,b,p,q,r;
    printf("Enter a:");
    scanf("%f",&a);
    printf("a=%.4f\n",a);
    b=(a*a*a*a*a*a*a*a*a*a*a*a*a*a*a*a);
     printf("p=%.4f\nq=%.4f\nr=%.4f",p=((b-1)/(a-1)),q=((b*(16*a-17)-1)/((a-1)*(a-1))),r=1/(1-a));

    return 0;
}
